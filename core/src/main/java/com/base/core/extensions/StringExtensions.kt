package com.base.core.extensions

import android.widget.TextView
import java.util.*

fun TextView.dateToString(date: Date, format: String) {
    /*
    val spf = SimpleDateFormat(format)
    text = spf.format(date)

     */
}

fun TextView.longToString(date: Long, format: String) {
    /*
    val spf = SimpleDateFormat(format)
    text = spf.format(Date(date * 1000))

     */
}
/*
fun String.
        toImageAddress(
    url: String = IMAGE_PREFIX,
    quality: String = IMAGE_QUALITY,
    size: String = IMAGE_SIZE
): String {
    return "$url$quality/$size/$this"


}

 */